Create GranisDialog01
    Events
    Conditions
    Actions
        Cinematic - Enable user control for (All players).
        Dialog - Clear GranisDialog01
        Dialog - Change the title of GranisDialog01 to Available Quests
        If (All Conditions are True) then do (Then Actions) else do (Else Actions)
            If - Conditions
                (QuestRoljin is discovered) Equal to False
                (QuestRoljin is completed) Equal to False
            Then - Actions
                Dialog - Create a dialog button for GranisDialog01 labelled Punish (Accept Ques...
                Set VariableSet GranisDialog01Punish = (Last created dialog Button)
                Dialog - Show GranisDialog01 for Player 1 (Red)
            Else - Actions
        If (All Conditions are True) then do (Then Actions) else do (Else Actions)
            If - Conditions
                (QuestRoljin is discovered) Equal to False
                (QuestRoljin is completed) Equal to False
            Then - Actions
                Dialog - Create a dialog button for GranisDialog01 labelled - Decline
                Set VariableSet GranisDialog01Decline = (Last created dialog Button)
                Dialog - Show GranisDialog01 for Player 1 (Red)
            Else - Actions
        If (All Conditions are True) then do (Then Actions) else do (Else Actions)
            If - Conditions
                (QuestRoljin is completed) Equal to True
            Then - Actions
                Dialog - Create a dialog button for GranisDialog01 labelled - Farewell
                Set VariableSet GranisDialog01Farewell = (Last created dialog Button)
                Dialog - Show GranisDialog01 for Player 1 (Red)
            Else - Actions
