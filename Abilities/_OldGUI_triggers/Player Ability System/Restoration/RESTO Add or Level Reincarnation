RESTO Add or Level Reincarnation
    Events
        Unit - A unit Acquires an item
    Conditions
        (Item-type of (Item being manipulated)) Equal to Reincarnation
    Actions
        -------- This is like the slow one, but it would increase the level of shockwave when you already have it --------
        If (All Conditions are True) then do (Then Actions) else do (Else Actions)
            If - Conditions
                (Level of RESTORATION_Reincarnation  for (Triggering unit)) Equal to 0
            Then - Actions
                Game - Display to (All players) the text: (New ability learned:  + (Name of (Item being manipulated)))
                Unit - Add RESTOTATION_ReincarnationDummy  to (Triggering unit)
                -------- Make ability permanent --------
                Custom script:   call UnitMakeAbilityPermanent(GetTriggerUnit(), true, 'A68A')
                Custom script:   call UnitMakeAbilityPermanent(GetTriggerUnit(), true, 'A68B')
                Sound - Play NewAbility <gen>
                If (All Conditions are True) then do (Then Actions) else do (Else Actions)
                    If - Conditions
                        (Triggering unit) Equal to Nazgrek
                    Then - Actions
                        Set VariableSet AbilityPointsNazgrek = (AbilityPointsNazgrek - 1)
                        Trigger - Run MultiboardUpdateAbilityPoints <gen> (checking conditions)
                    Else - Actions
                If (All Conditions are True) then do (Then Actions) else do (Else Actions)
                    If - Conditions
                        (Triggering unit) Equal to Zulkis
                    Then - Actions
                        Set VariableSet AbilityPointsZulkis = (AbilityPointsZulkis - 1)
                        Trigger - Run MultiboardUpdateAbilityPoints <gen> (checking conditions)
                    Else - Actions
            Else - Actions
                -------- Not using Increase level of ability --------
                -------- Because it seems to be able to go beyond the limit --------
                If (All Conditions are True) then do (Then Actions) else do (Else Actions)
                    If - Conditions
                        (Level of RESTORATION_Reincarnation  for (Triggering unit)) Less than 2
                    Then - Actions
                        Unit - Set level of RESTORATION_Reincarnation  for (Triggering unit) to ((Level of RESTORATION_Reincarnation  for (Triggering unit)) + 1)
                        If (All Conditions are True) then do (Then Actions) else do (Else Actions)
                            If - Conditions
                                (Triggering unit) Equal to Nazgrek
                            Then - Actions
                                Set VariableSet AbilityPointsNazgrek = (AbilityPointsNazgrek - 1)
                                Trigger - Run MultiboardUpdateAbilityPoints <gen> (checking conditions)
                            Else - Actions
                        If (All Conditions are True) then do (Then Actions) else do (Else Actions)
                            If - Conditions
                                (Triggering unit) Equal to Zulkis
                            Then - Actions
                                Set VariableSet AbilityPointsZulkis = (AbilityPointsZulkis - 1)
                                Trigger - Run MultiboardUpdateAbilityPoints <gen> (checking conditions)
                            Else - Actions
                    Else - Actions
                        -------- If unit already has ability maxed out, no ability points are reduced --------
                        Game - Display to (All players) the text: ((Name of (Item being manipulated)) +  |cFFFFCC00 Max level reached|r)
                        Sound - Play Error <gen>
